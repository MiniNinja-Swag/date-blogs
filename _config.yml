title: Our Date Blog
description: Memories from our adventures together
url: https://minininja-swag.github.io
baseurl: /date-blogs
theme: minima

collections:
  dates:
    output: true
    permalink: /dates/:title/

defaults:
  - scope: { path: "", type: dates }
    values:
      layout: date

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-paginate

paginate: 6
paginate_path: "/page:num"

exclude: [admin, node_modules, README.md]
